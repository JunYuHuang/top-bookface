<%# TODO - to test %>
<%# Does not matter which `User.id` is passed as the query parameter because only the hidden input values are used to create a follow request. %>
<%= form_with url: user_follow_path(user_id, follow_id), method: :delete do |form| %>
  <%= form.submit "Unfollow" %>
<% end %>
